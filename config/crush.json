{
  "$schema": "https://charm.land/crush.json",
  "model": "podllama-chat",
  "provider": "podllama",
  "system_prompt": "You are an autonomous AI coding assistant. When the user asks you to create, edit, or write files or run bash commands, you MUST execute the appropriate tool call natively (e.g. write, edit, bash) instead of printing JSON in text output. Always output your tool calls directly using the system tool calling schema.",
  "permissions": {
    "allowed_tools": [
      "view",
      "ls",
      "grep",
      "edit",
      "write",
      "bash"
    ]
  },
  "providers": {
    "podllama": {
      "type": "openai",
      "base_url": "${SERVER_URL}",
      "api_key": "${OPENAI_API_KEY}",
      "models": [
        {
          "id": "podllama-chat",
          "name": "PodLlama Chat (Qwen2.5-Coder-7B)",
          "context_window": 16384
        },
        {
          "id": "podllama-thinking",
          "name": "PodLlama Thinking (DeepSeek-R1-Distill-Qwen)",
          "context_window": 16384
        }
      ]
    }
  }
}
